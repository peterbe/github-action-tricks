name: Celebrate changed files

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  find-changed-files:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
        with:
          # Needed to detect changed files
          fetch-depth: 2

      - name: Get updated docs
        if: ${{ github.event_name == 'push' }}
        id: changed-files
        uses: tj-actions/changed-files@da093c1609db0edd0a037ce9664e135f74bf30d9 # pin @v40.2.0
        with:
          fetch_depth: 2
          files: docs/**/*.{md,mdx}
          files_ignore: docs/news/_template.md
          json: false

      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Debug what files changed
        env:
          FILES: ${{ steps.changed-files.outputs.all_changed_files }}
          DELETED_FILES: ${{ steps.changed-files.outputs.deleted_files }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          #ROOT: ../../..
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "${FILES}"
          python -c "import os; print(repr(os.environ['FILES']))"
